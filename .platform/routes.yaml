# https://{default}/:
#   type: upstream
#   upstream: "drupal:http"
#   id: "first"
#   cache:
#     enabled: true
#     cookies: ['/^SS?ESS/', '/^Drupal.visitor/']
# https://www.{default}/:
#   type: redirect
#   to: "https://{default}/"

https://api.{default}/:
  type: upstream
  upstream: "drupalb:http"
  id: "api"
  cache:
    enabled: true
    cookies: ['/^SS?ESS/', '/^Drupal.visitor/']
https://www.api.{default}/:
  type: redirect
  to: "https://api.{default}/"

# "https://www.{default}/":
#     type: upstream
#     upstream: "gatsby:http"

# "https://{default}/":
#     type: redirect
#     to: "https://www.{default}/"
